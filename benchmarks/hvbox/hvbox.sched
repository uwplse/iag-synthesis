traversal post {
  case Root { recur self.root; self.init(); }
  case HBox { recur self.childs; self.bound(); }
  case VBox { recur self.childs; self.bound(); }
  case Leaf { self.init(); }
} ;; traversal pre {
  case Root { recur self.root; }
  case HBox { self.place(); recur self.childs; }
  case VBox { self.place(); recur self.childs; }
  case Leaf { skip; }
}
